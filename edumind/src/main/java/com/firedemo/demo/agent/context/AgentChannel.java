package com.firedemo.demo.agent.context;

/** Identifies the trusted application channel that started an agent run. */
public enum AgentChannel {
    WEB,
    QQ_GROUP,
    QQ_PRIVATE,
    MCP,
    INTERNAL
}
